@import 'nib'

@import '_variables'

@import 'mixins/mixin'
@import 'mixins/grid'

global-reset()

body
  opacity: 0.5
  color: $color-primary
  background: #eee
  box-shadow: 1px 2px 3px
  
  h1
    background: red
    for i in 1..4
      @media (min-width: 2**(i+7)px)
        width: 100px*i
